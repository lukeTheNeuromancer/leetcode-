# 算法专题：LeetCode 扫描线（Sweep Line）与区间问题全解析

扫描线（Sweep Line）算法的核心思想是**将区间拆分为事件点（Event Points），按时间轴排序后顺次扫描**，从而把复杂的区间重叠问题降维成一维序列问题。

---

## 一、 核心题库分类与考点

### 1. 标准扫描线（事件点拆分 + 计数/高度维护）

拆分起点与终点事件，对事件点进行统一排序，在扫描过程中维护状态。

* **LintCode 391. 数飞机 (Count of Airplanes)** `热身`
  * **考点**：最基础的扫描线。起点记为 `+1`，终点记为 `-1`。
  * **核心**：同一时间点，**先处理降落（-1），再处理起飞（+1）**，即排序按 `(time, delta)` 升序。
* **LC 252. Meeting Rooms** `简单`
  * **考点**：判断区间是否存在任何重叠。按 `start` 排序后遍历，或扫描线看最大重叠数是否 $> 1$。
* **LC 253. Meeting Rooms II** `中等` `高频`
  * **考点**：求同一时间的最大重叠区间数（即需要的最少会议室/跑道数）。解法同 LintCode 391。
* **LC 218. The Skyline Problem (天际线问题)** `困难` ⭐
  * **考点**：扫描线 + 动态求最大值（大顶堆 / 平衡树）。
  * **技巧**：
    1. 将建筑物拆分为起点 `(left, -height)` 和终点 `(right, height)`。
    2. 使用负高度可以巧用默认排序：同一位置，**高楼起点排在前面**，防止矮楼产生虚假天际线。
    3. 维护当前最大高度，需要注意 PriorityQueue 在 Python/Java 中的删除开销（建议配合**延迟删除**或平衡树）。

---

### 2. 区间合并与裁剪（排序 + 顺序扫描）

无需拆分起点终点，按 `start` 升序排序后，用单指针顺次遍历合并。

* **LC 56. Merge Intervals (合并区间)** `中等` `必刷`
  * **考点**：按 `start` 排序，维护 `current_interval`。若 `next.start <= current.end` 则合并。
* **LC 57. Insert Interval (插入区间)** `中等`
  * **考点**：分三段处理（无重叠左侧、重叠合并段、无重叠右侧），或直接插入后做 LC 56。
* **LC 1272. Remove Interval (删除区间)** `中等`
  * **考点**：画图分析遮挡关系。被删除区间划分为左残留 `[start, remove_start]` 和右残留 `[remove_end, end]`。

---

### 3. 区间贪心与选择（按 End 排序）

* **LC 435. Non-overlapping Intervals (无重叠区间)** `中等` `高频`
  * **考点**：贪心算法。**按区间终点 `end` 升序排序**，优先选择最早结束的区间，给后续留出最多空间。
* **LC 1288. Remove Covered Intervals (删除被覆盖区间)** `中等`
  * **考点**：双关键码排序：**`start` 升序，`end` 降序**。保证起点相同时，范围更大的区间排在前面。

---

### 4. 动态区间维持（数据流问题）

* **LC 352. Data Stream as Disjoint Intervals** `困难` ⭐
  * **考点**：数据流插入与区间实时合并。
  * **解法**：
    * **Java**：使用 `TreeSet` / `TreeMap` 的 `floor()` / `ceiling()` 在 $O(\log N)$ 内查找邻近区间并合并。
    * **Python**：使用 `sortedcontainers` 的 `SortedDict` / `SortedList`；若仅使用标准库，可用 `bisect` 维持有序列表（插入为 $O(N)$）。

---

### 5. 双指针区间求交 / 多路归并

* **LC 1229. Meeting Scheduler (满足条件的交集)** `中等`
  * **考点**：双指针（`i`, `j`）分别指向两个人的空闲区间，计算 `intersection = min(end1, end2) - max(start1, start2)`。
* **LC 986. Interval List Intersections (区间列表的交集)** `中等`
  * **考点**：类似 LC 1229，求所有交集。移动终点较小的那一方指针（`end1 < end2 ? i++ : j++`）。
* **LC 759. Employee Free Time (员工空闲时间)** `困难` ⭐
  * **考点**：$N$ 个人的区间求交。
  * **解法**：可将所有人的区间扁平化后做 LC 56 求并集，空隙即为空闲；或利用 **PriorityQueue** 进行多路归并（$O(N \log K)$）。

---

## 二、 必背核心知识点与技巧

### 1. 扫描线事件排序规则（Comparator）
在拆分事件点 `(time, flag)` 时，**同一时间点的处理顺序决定了代码的正确性**：
* **重叠点算重叠**（如会议室）：先起飞/开始 (`+1`)，再降落/结束 (`-1`)。
* **重叠点不算重叠**（如数飞机）：先降落/结束 (`-1`)，再起飞/开始 (`+1`)。

### 2. 区间重叠四大判定关系

已知区间 $A = [s_1, e_1]$ 和区间 $B = [s_2, e_2]$（假设已按起点排序 $s_1 \le s_2$）：

1. **相交/重叠条件**：$\max(s_1, s_2) \le \min(e_1, e_2)$ 或 $s_2 \le e_1$。
2. **重叠区域范围**：$[\max(s_1, s_2), \min(e_1, e_2)]$。
3. **包含关系**（$A$ 包含 $B$）：$s_1 \le s_2$ 且 $e_1 \ge e_2$。
4. **不相交条件**：$e_1 < s_2$。

### 3. 数据结构开销陷阱
* **Java `PriorityQueue.remove(Object)`**：时间复杂度为 $O(N)$，频发删除会导致 TLE。
* **Python `heapq`**：无内置任意删除功能。
* **应对方案**：使用**延迟删除（Lazy Deletion）**，即用 Hash 表/Set 记录废弃节点，出堆顶时再真正弹出；或直接使用**平衡二叉树**（Java `TreeSet` / Python `SortedList`）。
